<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout android:background="@drawable/background_main"
                android:id="@+id/activity_level_selection"
                android:layout_height="match_parent"
                android:layout_width="match_parent"
                xmlns:android="http://schemas.android.com/apk/res/android"
                xmlns:tools="http://schemas.android.com/tools"
                tools:context="himka.gamehimka.SubLevelSelectionActivity">

    <ImageView
        android:adjustViewBounds="true"
        android:id="@+id/sub_level"
        android:src="@drawable/ic_easy"
        android:layout_centerHorizontal="true"
        android:layout_gravity="center_horizontal"
        android:layout_height="wrap_content"
        android:layout_margin="16dp"
        android:layout_width="220dp"/>

    <ImageView
        android:id="@+id/button_home"
        android:layout_height="wrap_content"
        android:layout_margin="8dp"
        android:layout_width="wrap_content"
        android:src="@drawable/ic_home"/>

    <ImageView
        android:id="@+id/button_back"
        android:layout_alignParentBottom="true"
        android:layout_height="wrap_content"
        android:layout_margin="8dp"
        android:layout_width="wrap_content"
        android:src="@drawable/ic_back"/>

    <LinearLayout
        android:layout_below="@id/sub_level"
        android:layout_height="match_parent"
        android:layout_width="match_parent"
        android:orientation="horizontal"
        android:weightSum="4">

        <View
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:layout_width="0dp"/>

        <!--we don't use fragment here, because it will error if assigned programmatically-->
        <FrameLayout
            android:id="@+id/fragment"
            android:layout_height="match_parent"
            android:layout_weight="2"
            android:layout_width="0dp"/>

    </LinearLayout>

</RelativeLayout>
